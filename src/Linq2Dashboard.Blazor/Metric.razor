@typeparam T
@inherits DashboardComponentBase<T>

<MetricTile Key="@Key" Title="@(Title ?? Current.Title)" Value="@Formatter.FormatMetric(Current)" Share="@ShareText" IsEmpty="@(!Current.HasValue)" ChildContent="Template" />
